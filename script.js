// Operator Functions
function add(a, b) {
  return (a + b);
}

function subtract(a, b) {
 return (a - b);
}

function multiply(a, b) {
  return (a * b);
}

function divde(a, b) {
  if (b === 0) {
    return `Nice try -> cannot divide by zero`;
  }
  else {
    return (a / b);
  }
}

